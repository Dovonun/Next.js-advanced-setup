// File to test eslint(enforce const)

// Change this to let and an eslint error should appear.
// The error should be resolfed on safe if configured in "settings.json".
export const CONSTITIS = '1.0.0';
